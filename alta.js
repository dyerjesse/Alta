const csv = require('csv-parse')
const prompt = require('prompt')
const alert = require('alert')



userLevel = prompt.get("What level rider are you? novice, intermediate, or expert?", "");

alert(userLevel)

